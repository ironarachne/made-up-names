/**
 * Generates a name from a set of patterns.
 *
 * @param {string[]} patterns - The patterns to use
 * @returns {string} The generated name
 */
export declare function generate(patterns: string[], seed?: number, count?: number): string[];
